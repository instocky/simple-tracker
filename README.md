# Simple Time Tracker

Автоматический трекер времени работы над проектами с поддержкой **неограниченной иерархии**, **пассивного отслеживания активности** и **интеллектуального анализа продуктивности**.

## 🚀 Основные возможности

- ✅ **Иерархические проекты** с неограниченной вложенностью
- ✅ **Автоматический трекинг** времени каждые 5 минут
- ✅ **Интеллектуальный мониторинг** активности пользователя (Windows API)
- ✅ **Пассивное отслеживание** всей активности за компьютером
- ✅ **Анализ продуктивности** с метриками и рекомендациями
- ✅ **Короткие команды** для быстрого управления проектами
- ✅ **Агрегированное время** с учетом всех дочерних проектов
- ✅ **Глобальный доступ** из любой папки через `tracker.bat`
- ✅ **CLI интерфейс** с расширенными командами
- ✅ **Обратная совместимость** со старыми данными
- ✅ **Автоматическая миграция** данных с backup
- ✅ **Производительность** <500ms для трекера

## 📁 Архитектура проекта

```
simple-tracker/
├── core/                    # 🔧 Core модули
│   ├── transliteration.py   # Транслитерация RU→EN
│   ├── compatibility.py     # Поддержка старого формата
│   ├── hierarchy.py         # Алгоритмы иерархии
│   ├── active.py           # Мониторинг активности пользователя
│   ├── notifications.py    # Система уведомлений о перерывах
│   └── console_utils.py     # Unicode-безопасный вывод
├── tests/                   # 🧪 Тестовая инфраструктура
│   ├── test_core.py         # Тесты core модулей
│   ├── test_integration_final.py # Интеграционные тесты
│   ├── test_tracker_new.py  # Тесты трекера
│   └── run_all.py           # Запуск всех тестов
├── legacy/                  # 📦 Backup оригиналов
├── tracker_quick.py         # ⚡ Трекер с пассивным отслеживанием
├── project_manager.py       # 🎛️ CLI с короткими командами
├── tracker.bat             # 🌐 Глобальный запуск из любой папки
├── db.json                  # 🗄️ БД с пассивным отслеживанием
├── run_silent.vbs          # ⏰ Windows планировщик
└── specification_*.md      # 📋 Техзадания
```

## 🎯 Быстрый старт

### Глобальный доступ
```bash
# Скопируйте tracker.bat в папку из PATH или используйте полный путь
tracker help                         # Справка по командам
tracker list                         # Список всех проектов
```

### Короткие команды (NEW!)
```bash
tracker -a "project-name"            # Активировать проект
tracker -p "project-name"            # Приостановить проект  
tracker -c "project-name"            # Завершить проект
tracker -r "project-name"            # Архивировать проект
```

### Анализ продуктивности (NEW!)
```bash
tracker passive                      # Статистика пассивного отслеживания
tracker timeline                     # Временная шкала активности
tracker passive 2025-06-09          # Статистика за конкретную дату
```

## 📊 Пассивное отслеживание активности (NEW!)

### Что отслеживается автоматически:
- 💻 **Общая активность** за компьютером (мышь, клавиатура)
- 📈 **Проектное время** (активный проект + пользователь активен)
- ❓ **Непроектная работа** (активность без проекта)
- 😴 **Периоды простоя** (>5 минут бездействия)

### Пример анализа:
```bash
tracker passive
```
```
=== Пассивная статистика за 2025-06-09 ===

Время за компьютером:       8ч 0м (66.7% от рабочего дня)
Проектная работа:           4ч 0м (50.0% от времени за ПК)
Непроектная активность:     2ч 0м (25.0% от времени за ПК)  
Простой/перерывы:           2ч 0м (16.7% от рабочего дня)

Продуктивность: 50.0% (проектная работа / общее время за ПК)

Нормальная продуктивность. Баланс между проектами и другими задачами.
```

### Временная шкала:
```bash
tracker timeline
```
```
=== Временная шкала активности 2025-06-09 ===
Легенда: [P] Проект | [A] Активность | [I] Простой | [-] Нет данных

08:00 PPPPAAAA----
09:00 PPPPPPPPPPPP  
10:00 AAAAAAAAA---
11:00 IIII--------
```

## 🎛️ Полный список команд

### Быстрые команды (рекомендуется)
```bash
tracker -a "project"                 # Активировать проект
tracker -p "project"                 # Приостановить  
tracker -c "project"                 # Завершить
tracker -r "project"                 # Архивировать
```

### Просмотр и анализ
```bash
tracker list                         # Все проекты
tracker tree                         # Древовидная структура
tracker info                         # Информация о БД
tracker passive [дата]               # Анализ продуктивности
tracker timeline [дата]              # Временная шкала
```

### Управление проектами
```bash
tracker create "Project Name"                    # Корневой проект
tracker create "SubProject" --parent "parent"    # Дочерний проект
tracker active "project"                         # Активировать (длинная форма)
tracker status "project" completed               # Установить статус
```

### Системные команды
```bash
tracker migrate                      # Миграция в новый формат
tracker help                         # Справка по всем командам
```

## 🔍 Интеллектуальный мониторинг

### Активность пользователя
- ✅ **Windows API интеграция** для определения реального времени работы
- ✅ **Настраиваемый порог idle** (по умолчанию 5 минут)
- ✅ **Отслеживание активного окна** для контекста
- ✅ **Автоматический пропуск** записи времени при неактивности

### Настройки в db.json:
```json
{
  "meta": {
    "idle_threshold_seconds": 300,     // 5 минут до idle
    "passive_tracking": {
      "enabled": true,                 // Пассивное отслеживание
      "track_idle_periods": true,      // Записывать простои
      "track_non_project_activity": true // Записывать непроектную работу
    }
  }
}
```

## 📊 Структура данных (обновлена)

### Новый формат с пассивным отслеживанием
```json
{
  "meta": {
    "work_hours": {"start": "08:00", "end": "20:00"},
    "idle_threshold_seconds": 300,
    "passive_tracking": {
      "enabled": true,
      "daily_masks": {
        "2025-06-09": {
          "computer_activity": "111111111100000011111111...",
          "project_activity":  "110010000100010000000000...",
          "idle_periods":      "000000000011111100000000...",
          "untracked_work":    "001101111000000011111111..."
        }
      },
      "analysis": {
        "total_computer_time_minutes": 480,
        "total_project_time_minutes": 240,
        "productivity_ratio": 0.5
      }
    }
  },
  "projects": [
    {
      "id": "exlibrus",
      "path": "exlibrus",
      "title": "ExLibrus", 
      "status": "active",
      "total_minutes": 335,
      "aggregated_minutes": 520,
      "daily_masks": {
        "2025-06-09": "110010000100010000000000..."
      }
    }
  ]
}
```

## ⚡ Принцип работы

### 1. Автоматический трекинг
```
Каждые 5 минут → Планировщик → tracker_quick.py
                             ↓
   Проверка активности пользователя (Windows API)
                             ↓
        Активен? → Записать время проекта + пассивные данные
        Неактивен? → Записать только пассивные данные (простой)
```

### 2. Пассивное отслеживание
```
computer_activity = любая активность за ПК
project_activity = активность + есть активный проект  
idle_periods = бездействие >5 минут
untracked_work = активность без проекта
```

### 3. Анализ продуктивности
```
Продуктивность = project_time / computer_time * 100%

< 30% = Низкая (много администрирования)
30-70% = Нормальная (баланс проекты/другие задачи)  
> 70% = Высокая (фокус на проектах)
```

## 🧪 Тестирование

```bash
# Запуск всех тестов
python tests/run_all.py

# Тестирование конкретных модулей
python tests/test_core.py
python tests/test_integration_final.py
python tests/test_tracker_new.py
```

## 📝 Практические примеры

### Типичный рабочий день:
```bash
# Утром
tracker -a "main-project"            # Активируем основной проект

# В процессе работы  
tracker -p "main-project"            # Пауза для встречи
tracker -a "admin-tasks"             # Переключаемся на админ задачи
tracker -a "main-project"            # Возвращаемся к основному

# В конце дня
tracker -p "main-project"            # Останавливаем
tracker passive                      # Смотрим статистику
```

### Анализ результатов:
```
Время за компьютером:    7ч 30м (62.5% от рабочего дня)
Проектная работа:        5ч 15м (70% продуктивность)
Непроектная активность:  1ч 30м (встречи, почта)
Простой/перерывы:        45м (обед, перерывы)

Отличная продуктивность! Много времени на проектную работу.
```

### Создание иерархии проектов:
```bash
tracker create "ExLibrus"                           # Основной проект
tracker create "Frontend" --parent "exlibrus"       # Подпроект
tracker create "Components" --parent "exlibrus/frontend"  # Вложенный
tracker -a "exlibrus/frontend/components"           # Активируем глубокий уровень
tracker tree                                        # Смотрим структуру
```

## 🔄 Миграция и совместимость

### Автоматическая инициализация
- При первом запуске создается структура пассивного отслеживания
- Существующие проекты продолжают работать без изменений
- Короткие команды доступны сразу после обновления

### Обратная совместимость
- ✅ Все старые команды работают как раньше
- ✅ Существующие данные сохраняются
- ✅ Планировщик не требует изменений
- ✅ Graceful fallback при отсутствии новых модулей

## 🛠️ Настройка глобального доступа

### Вариант 1: Копирование в PATH
```bash
copy tracker.bat C:\Windows\System32\
# Теперь можно использовать tracker из любой папки
```

### Вариант 2: Добавление в PATH
1. Добавить `C:\Projects\Bash\0604_simple-tracker\` в системную переменную PATH
2. Перезапустить консоль
3. Использовать `tracker` из любого места

### Вариант 3: Алиасы PowerShell (опционально)
```powershell
# Добавить в $PROFILE
function ta { tracker -a $args }    # ta "project"
function tp { tracker -p $args }    # tp "project"  
function tl { tracker list }        # tl
function tt { tracker tree }        # tt
function ts { tracker passive }     # ts (stats)
```

## 🎯 Новые возможности (v2.0)

### ✨ Добавлено в этой версии:
1. **Короткие команды** (`-a`, `-p`, `-c`, `-r`) для быстрого управления
2. **Пассивное отслеживание** всей активности за компьютером
3. **Анализ продуктивности** с метриками и рекомендациями  
4. **Временная шкала** активности по часам
5. **Глобальный доступ** через `tracker.bat` из любой папки
6. **Интеллектуальный мониторинг** активности через Windows API
7. **Автоматические рекомендации** по оптимизации рабочего процесса

### 🚀 Roadmap (v3.0):
- Веб-интерфейс с графиками продуктивности
- Экспорт отчетов в Excel/PDF
- Интеграция с календарем и задачами
- Звуковые уведомления о перерывах
- Командная работа и синхронизация

---

**Simple Time Tracker v2.0** - от простого счетчика времени до **комплексной системы анализа продуктивности** с глубокой аналитикой рабочих паттернов и автоматическими рекомендациями по оптимизации.

## 📊 Структура данных

### Новый формат БД (поддерживает иерархию)
```json
{
  "meta": {
    "work_hours": {"start": "08:00", "end": "20:00"},
    "time_tracking": {"interval_minutes": 5, "total_daily_slots": 144}
  },
  "projects": [
    {
      "id": "exlibrus",
      "path": "exlibrus", 
      "title": "ExLibrus",
      "status": "active",
      "fill_color": "#4CAF50",
      "total_minutes": 335,          // собственное время
      "aggregated_minutes": 520,     // общее время с дочерними
      "daily_masks": {
        "2025-06-07": "110010000100010000000000..."
      }
    },
    {
      "id": "exlibrus-frontend",
      "path": "exlibrus/frontend",
      "title": "Frontend", 
      "status": "paused",
      "total_minutes": 120,
      "aggregated_minutes": 185,     // 120 + 65 (дочерние)
      "daily_masks": {...}
    }
  ]
}
```

### Ключевые поля
- **`id`**: Уникальный идентификатор (транслитерация title)
- **`path`**: Иерархический путь проекта (`parent/child`)  
- **`total_minutes`**: Собственное время проекта
- **`aggregated_minutes`**: Общее время (свое + всех дочерних)

## 🎛️ CLI команды

### Просмотр проектов
```bash
list                # Адаптивный список (старый/новый формат)
tree                # Древовидная структура с иерархией
info                # Информация о БД и статистика
```

### Управление проектами  
```bash
active <идентификатор>         # Сделать проект активным
status <идентификатор> <статус> # Установить статус
create <название>              # Создать корневой проект
create <название> --parent <path> # Создать дочерний проект
```

### Системные команды
```bash
migrate             # Миграция в новый формат
help                # Справка по командам
```

### Статусы проектов
- `active` - текущий проект (идет трекинг времени)
- `paused` - приостановлен  
- `completed` - завершен успешно
- `archived` - архивирован

## ⚡ Принцип работы

### Трекинг времени
1. **Битовые маски**: каждый 5-минутный интервал = 1 бит (08:00-20:00 = 144 бита)
2. **Автоматический трекинг**: планировщик запускает `tracker_quick.py` каждые 5 минут
3. **Иерархическое обновление**: время автоматически агрегируется вверх по дереву
4. **Производительность**: трекер работает <500ms без зависания

### Агрегированное время
```
ExLibrus (50 мин)                    → aggregated: 230 мин
├── Frontend (120 мин)               → aggregated: 180 мин  
│   └── Components (60 мин)          → aggregated: 60 мин
└── Backend (100 мин)                → aggregated: 100 мин
```

**Расчет**: ExLibrus = 50 (свои) + 180 (Frontend) + 100 (Backend) = 330 мин

## 🧪 Тестирование

```bash
# Запуск всех тестов
python tests/run_all.py

# Тестирование конкретных модулей
python tests/test_core.py
python tests/test_integration_final.py
python tests/test_tracker_new.py
```

## 📝 Практические примеры

### Типичный рабочий день:
```bash
# Утром
tracker -a "main-project"            # Активируем основной проект

# В процессе работы  
tracker -p "main-project"            # Пауза для встречи
tracker -a "admin-tasks"             # Переключаемся на админ задачи
tracker -a "main-project"            # Возвращаемся к основному

# В конце дня
tracker -p "main-project"            # Останавливаем
tracker passive                      # Смотрим статистику
```

### Анализ результатов:
```
Время за компьютером:    7ч 30м (62.5% от рабочего дня)
Проектная работа:        5ч 15м (70% продуктивность)
Непроектная активность:  1ч 30м (встречи, почта)
Простой/перерывы:        45м (обед, перерывы)

Отличная продуктивность! Много времени на проектную работу.
```

### Создание иерархии проектов:
```bash
tracker create "ExLibrus"                           # Основной проект
tracker create "Frontend" --parent "exlibrus"       # Подпроект
tracker create "Components" --parent "exlibrus/frontend"  # Вложенный
tracker -a "exlibrus/frontend/components"           # Активируем глубокий уровень
tracker tree                                        # Смотрим структуру
```

### Результат иерархии:
```
ExLibrus [пауза]
  5ч35м собств. / 6ч40м общее
+- Frontend [пауза]
     2ч20м собств. / 3ч5м общее  
   +- Components [активен]
        0ч45м собств. / 0ч45м общее
+- Backend [пауза]
     1ч10м собств. / 1ч10м общее
```

## 🔄 Миграция и совместимость

### Автоматическая инициализация
- При первом запуске создается структура пассивного отслеживания
- Существующие проекты продолжают работать без изменений
- Короткие команды доступны сразу после обновления

### Обратная совместимость
- ✅ Все старые команды работают как раньше
- ✅ Существующие данные сохраняются
- ✅ Планировщик не требует изменений
- ✅ Graceful fallback при отсутствии новых модулей

### Миграция данных
1. **Автоматическое определение** формата БД
2. **Безопасная миграция** с созданием backup
3. **Генерация полей** id, path, aggregated_minutes
4. **Валидация целостности** иерархии

## 🛠️ Настройка глобального доступа

### Вариант 1: Копирование в PATH
```bash
copy tracker.bat C:\Windows\System32\
# Теперь можно использовать tracker из любой папки
```

### Вариант 2: Добавление в PATH
1. Добавить `C:\Projects\Bash\0604_simple-tracker\` в системную переменную PATH
2. Перезапустить консоль
3. Использовать `tracker` из любого места

### Вариант 3: Алиасы PowerShell (опционально)
```powershell
# Добавить в $PROFILE
function ta { tracker -a $args }    # ta "project"
function tp { tracker -p $args }    # tp "project"  
function tl { tracker list }        # tl
function tt { tracker tree }        # tt
function ts { tracker passive }     # ts (stats)
```

## 🎯 Новые возможности (v2.0)

### ✨ Добавлено в этой версии:
1. **Короткие команды** (`-a`, `-p`, `-c`, `-r`) для быстрого управления
2. **Пассивное отслеживание** всей активности за компьютером
3. **Анализ продуктивности** с метриками и рекомендациями  
4. **Временная шкала** активности по часам
5. **Глобальный доступ** через `tracker.bat` из любой папки
6. **Интеллектуальный мониторинг** активности через Windows API
7. **Автоматические рекомендации** по оптимизации рабочего процесса

### 🚀 Roadmap (v3.0):
- Веб-интерфейс с графиками продуктивности
- Экспорт отчетов в Excel/PDF
- Интеграция с календарем и задачами
- Звуковые уведомления о перерывах
- Командная работа и синхронизация

## 🛠️ Разработка

### Модульная архитектура
- **`core/transliteration.py`**: Конвертация русских названий в path/id
- **`core/compatibility.py`**: Поддержка старого формата
- **`core/hierarchy.py`**: Алгоритмы работы с иерархией проектов
- **`core/active.py`**: Мониторинг активности пользователя (Windows API)
- **`core/notifications.py`**: Система уведомлений о перерывах
- **`core/console_utils.py`**: Безопасный вывод в Windows консоль

### Маркеры legacy кода
Весь код совместимости помечен `# TODO: LEGACY_SUPPORT` для последующего удаления после полной миграции.

### Настройка планировщика
1. Создать задачу в планировщике Windows
2. Запускать `run_silent.vbs` каждые 5 минут  
3. Трекер автоматически отмечает время активного проекта
4. Пассивное отслеживание работает автоматически

---

**Simple Time Tracker v2.0** - от простого счетчика времени до **комплексной системы анализа продуктивности** с глубокой аналитикой рабочих паттернов и автоматическими рекомендациями по оптимизации.
